server.port=8080

# WebSocket + HTTP logs so you can see the handshake/frames
logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.web=INFO

# (optional) tiny QoL
server.compression.enabled=true
